{
  "name": "vmx",
  "version": "2.0.0",
  "module": "index.ts",
  "type": "module",
  "description": "Download ISOs and manage VMs from the terminal",
  "repository": "https://github.com/mnsdojo/vmx",
  "license": "MIT",
  "scripts": {
    "start": "bun run cli.ts",
    "dev": "bun run cli.ts",
    "build": "bun build cli.ts --compile --outfile vmx",
    "build:macos-x64": "bun build cli.ts --compile --outfile vmx-darwin-x64",
    "build:macos-arm64": "bun build cli.ts --compile --outfile vmx-darwin-arm64",
    "build:linux-x64": "bun build cli.ts --compile --outfile vmx-linux-x64",
    "prepublish": "bun run build"
  },
  "bin": {
    "vmx": "cli.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "boxen": "^8.0.1",
    "chalk": "^5.6.2",
    "cli-table3": "^0.6.5"
  }
}
